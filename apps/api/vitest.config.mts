/**
 * @file apps/api/vitest.config.mts
 * Vitest config for the API gateway (Node environment).
 */

import { createNodeVitestConfig } from "@tradingagents/vitest-config/node";

export default createNodeVitestConfig();
